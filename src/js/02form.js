function textCounter(ev) {
    let text = text.value;
    for (let i = 1; i < text.length; i++) {
        if (text.length === 40) {
            //debe añadir un salto de línea al texto con \n
        }
    }

}